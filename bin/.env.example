# Tickisinator Environment Variables
#
# Copy this file to .env and fill in your actual API key.
# The wrapper script (bin/tickisinator) will automatically load these
# variables if they're not already set in your environment.
#
# DO NOT commit .env to version control!

# Financial Modeling Prep API Key
# Get yours at: https://site.financialmodelingprep.com/developer/docs
FMP_API_KEY=your_api_key_here

# Optional: Override database location
# TICKISINATOR_DB_PATH=~/.config/tickisinator/tickisinator.db

# Optional: Enable verbose logging
# TICKISINATOR_VERBOSE=1
